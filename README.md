# ing1-s2-ts-tp2

## Notes rappel de cours

### Analogie

- Appliquer TF à signal donne fréquence
- Appliquer TF inverse à fréquence donne le signal

x(t) -TF-> X(f)
X(f) -TFI-> x(t)

### Passage en numérique/digital

TFD = TF discrète = application de TF en numérique
TFR = TF rapide = FFT = TFD en binaire (plus rapide)

t=nTs

Objectif : echantilloner X(f)
